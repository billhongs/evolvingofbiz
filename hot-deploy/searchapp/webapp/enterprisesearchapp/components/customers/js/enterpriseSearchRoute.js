
var enterpriseSearch = angular.module('enterpriseSearch', ['ngRoute','enterpriseSearch.controllers', 'enterpriseSearch.directives'])

/*enterpriseSearch.config(function($routeProvider) {
  $routeProvider
  // route for the home page
  .when ('/Home', {
        templateUrl: '../index.html',
        controller: 'enterpriseSearchController'
  })
  .otherwise({
    redirectTo: '/Home'
  });
});

*/